<Window x:Class="EditCollection.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:EditCollection.ViewModels"
        xmlns:views="clr-namespace:EditCollection.Views"
        Title="Удаление, копирование, редактирование элементов"
        Height="400"
        Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ItemTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <views:CollectionViewer ItemsSource="{Binding OperationViews}"
                                SelectedItem="{Binding SelectedView, Mode=OneWayToSource}" />
        <Button Grid.Column="1"
                VerticalAlignment="Center"
                Command="{Binding CopyCommand}"
                HorizontalAlignment="Center">Копировать</Button>

        <views:CollectionViewer Grid.Column="2"
                                ItemsSource="{Binding ViewsCopy}" />
    </Grid>
</Window>